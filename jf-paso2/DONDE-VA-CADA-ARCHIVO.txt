JUSTICIAFÁCIL · PASO 2 (conectar Supabase + Expedientes reales)

ORDEN:

1) Primero corre el SQL en Supabase:
   - Abre Supabase -> SQL Editor -> pega "paso2_lectura_anon.sql" -> Run.
   - Esto permite que la app LEA tus casos.

2) Luego sube estos 2 archivos al repo, en estas rutas EXACTAS:

   src/lib/supabase.ts            ← ARCHIVO NUEVO (ya trae tu URL y tu llave)
   src/routes/expedientes.index.tsx  ← REEMPLAZA el que ya existe

3) Netlify compila solo. Abre la app -> entra a "Expedientes":
   deben aparecer tus 99 casos reales, con buscador y filtros por estado y prioridad.

NOTAS:
- La llave pública ya viene puesta dentro de supabase.ts. No tienes que escribir nada.
- Por ahora la tabla queda legible sin login. Cuando agreguemos el login (fase siguiente),
  la cerramos para que solo entre tu equipo.
